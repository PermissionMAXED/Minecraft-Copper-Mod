package net.sonic0810.kupferbienen.feature.machines;

/**
 * Copper machinery: processing blocks (e.g. the Wabenpresse) for the mega-expansion.
 * Blocks/items register into {@code ModCreativeTab.MASCHINEN_KEY} (the tab's lazy icon
 * already points at {@code kupferbienen:wabenpresse}). Assets are generated by
 * devtools/gen/kupferbienen_gen.py ({@code MACHINE_BLOCKS} table,
 * {@code emit_machine_block} emitter).
 *
 * <p>Skeleton: content is added by later expansion workers.
 */
public final class MachinesFeature {
	private MachinesFeature() {
	}

	public static void init() {
		// Skeleton: machinery blocks/items/block entities register here.
	}
}
