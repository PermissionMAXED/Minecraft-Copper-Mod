package net.sonic0810.copperinferno.feature.slagfiends;

import net.minecraft.entity.EntityType;
import net.minecraft.entity.SpawnReason;
import net.minecraft.entity.mob.HoglinEntity;
import net.minecraft.entity.passive.PassiveEntity;
import net.minecraft.server.world.ServerWorld;
import net.minecraft.world.World;

/**
 * Slagtusk Brute - a slab-shouldered brute with tusks of hardened slag, native to the Inferno dimension. Behavior is pure
 * vanilla HoglinEntity; the stat distinction comes from the tuned default
 * attributes registered in {@link SlagFiendsFeature}. Drops
 * Slagtusk Fragment ({@code loot_table/entities/slagtusk_brute.json}).
 *
 * <p>Generated by {@code devtools/gen/slagfiends_gen.py}; do not edit by hand.
 */
public class SlagtuskBruteEntity extends HoglinEntity {
	public SlagtuskBruteEntity(EntityType<? extends HoglinEntity> type, World world) {
		super(type, world);
	}

	/**
	 * Vanilla {@code HoglinEntity.canConvert()} (public, javap-verified) gates the zoglin
	 * conversion on {@code !world.getDimension().piglinSafe()}; slag-fiend hoglins never
	 * convert, in the Inferno or anywhere else.
	 */
	@Override
	public boolean canConvert() {
		return false;
	}

	/**
	 * Vanilla {@code HoglinEntity.createChild} hard-codes {@code EntityType.HOGLIN}
	 * (bytecode-verified: create -> setPersistent -> return), so bred Slagtusk Brutes
	 * would produce vanilla hoglins. Covariant override, same create/setPersistent flow.
	 */
	@Override
	public SlagtuskBruteEntity createChild(ServerWorld world, PassiveEntity entity) {
		SlagtuskBruteEntity child = SlagFiendsFeature.SLAGTUSK_BRUTE.create(world, SpawnReason.BREEDING);
		if (child != null) {
			child.setPersistent();
		}
		return child;
	}
}
