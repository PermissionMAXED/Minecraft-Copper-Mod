package net.sonic0810.copperinferno.feature.wildworld;

/**
 * v4 "Wild World" feature — SKELETON (WP1). Content lands in a later work package;
 * {@code init()} is already wired into {@code CopperInferno.onInitialize()} so the v4 wave
 * ordering is fixed up front.
 */
public final class WildWorldFeature {
	private WildWorldFeature() {
	}

	public static void init() {
	}
}
